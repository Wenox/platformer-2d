#pragma once

namespace res {
    enum class Texture {
        BlockRed,
        BlockBlue,
        BlockBrown,
        BlockGray,
        BlockGreen,
        BlockPurple,
        BlockYellow,

        Player,
        PlayerLeft,
        PlayerRight,

        Objective,

        Heart,
        HeartEmpty,

        BgClouds,
        BgPaused,

        Spike
    };

    enum class Sound {
        Bullet,
        Bing,
        Death
    };

    enum class Music {
        Ambient
    };
}