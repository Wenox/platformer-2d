#pragma once

namespace res {
    enum class Texture {
        Block,
        Gray,
        Green,
        Orange,
        Wizard
    };

    enum class Sound {
        Bullet,
        Poof,
        Bing
    };

    enum class Music {
        Ambient
    };
}