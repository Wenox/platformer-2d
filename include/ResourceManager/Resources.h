#pragma once

namespace res {
    enum class Texture {
        Gray,
        Green,
        Orange,
        Wizard
    };

    enum class Sound {
        Bullet,
        Poof,
        Bing
    };

    enum class Music {
        Ambient
    };
}