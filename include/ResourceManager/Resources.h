#pragma once

namespace res {
    enum class Texture {
        BlockRed,
        BlockBlue,
        BlockBrown,
        BlockGray,
        BlockGreen,
        BlockPurple,
        BlockYellow,
        Wizard,
        Objective
    };

    enum class Sound {
        Bullet,
        Bing
    };

    enum class Music {
        Ambient
    };
}