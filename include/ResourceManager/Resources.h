#pragma once

namespace res {
    enum class Texture {
        BlockRed,
        BlockBlue,
        BlockBrown,
        BlockGray,
        BlockGreen,
        BlockPurple,
        BlockYellow,

        Player,
        PlayerLeft,
        PlayerRight,

        Objective,

        Heart
    };

    enum class Sound {
        Bullet,
        Bing
    };

    enum class Music {
        Ambient
    };
}