#pragma once

namespace res {
    enum class Texture {
        BlockRed,
        BlockBlue,
        BlockBrown,
        BlockGray,
        BlockGreen,
        BlockPurple,
        BlockYellow,

        Player,
        PlayerLeft,
        PlayerRight,

        Objective,

        Heart,

        Spike,
        SpikeLeft,
        SpikeRight,
        SpikeTop
    };

    enum class Sound {
        Bullet,
        Bing
    };

    enum class Music {
        Ambient
    };
}